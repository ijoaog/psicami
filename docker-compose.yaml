services:
  psicami:
    build: .
    container_name: psicami
    expose:
      - "3001"
    environment:
      - NODE_ENV=production
      - PORT=3001
    networks:
      - web
    restart: unless-stopped

networks:
  web:
    external: true